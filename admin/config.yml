local_backend: true 

backend:
  name: git-gateway
  branch: main # Branch to update (optional; defaults to master) 

media_folder: "./assets/images/uploads" 

collections:
  - label: "Status"
    name: "status"
    files:
      - label: "Status"
        name: "Status"
        file: "_status/status.yml" 
        fields: 
          - 
            label: "Select Status" 
            name: "select_status" 
            widget: "select" 
            options: ["Латето е тука", "Латето я няма"] 
